# TTS Engine: 'edge' (free, reliable) or 'cantonese-ai' (better quality, may rate limit)
# Default: edge
TTS_ENGINE=edge

# Edge TTS Voice (when using edge engine)
# Options: zh-HK-WanLungNeural (Male), zh-HK-HiuGaaiNeural (Female), zh-HK-HiuMaanNeural (Female)
EDGE_TTS_VOICE=zh-HK-WanLungNeural

# Cantonese.ai API Key (required for STT; optional for TTS if using edge engine)
# Get your API key from: https://cantonese.ai/api-keys
CANTONESE_AI_API_KEY=your_api_key_here

# Anthropic API Key (for conversational AI)
# Get your API key from: https://console.anthropic.com/
# Optional: If not set, uses simple pattern-matching responses
ANTHROPIC_API_KEY=your_anthropic_key_here

# Server Port (optional, defaults to 3000)
PORT=3000
